# HashMap
```
hm.clear();

hm.clone();

boolean hm.containsKey(obj key);

//?? time complexity
boolean b = hm.containsValue(obj val);

hm.get(Obj key);

hm.put(key, value);

hm.size();

hm.remove(key);

hm.getOrDefault(key);

vv = hm.computeIfAbsent(key, (key) -> value);
```
