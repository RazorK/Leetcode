### Queue
```
// initialize a Queue
Queue<Integer> q = new LinkedList<>();

// add
boolean b = q.add(e);

// empty
boolean b = q.isEmpty();

// get the head and remove, and return null if empty.
E e = q.poll();

// size
int size = q.size();
```
