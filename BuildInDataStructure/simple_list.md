# arrays
```
l.length
Arrays.sort(list)
//init 2-d Arrays
int [][] list = new int [][] {
  {1,2,3},
  {4,5,6}
}

Arrays.asList()
//坑， 如果要使用， 传入的不能是基本类型array
// 而且返回的array list 不能进行增删操作

Arrays.fill(array, -1);
```
