# Collections
```
// Reverse list type
Collections.reverse(aList);

Collections.max(list, comparator);

Collections.sort(list, comparator);

// reversed natural order comparator
Collections.reverseOrder()

```
