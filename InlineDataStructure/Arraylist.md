### ArrayList
```
arraylist.size()

arraylist.toArray()

arraylist.get(id)

arraylist.set(id, obj)

al.remove(id)

al.remove(obj)

al.clear()

al.add(obj)

al.add(id, obj) = set

al.addAll({id,} Set s)
```
